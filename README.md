# imageconvertfastapi

Simple JPG to PDF converter, built with FastAPI.

## Run

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Then open http://127.0.0.1:8000/jpgtopdf/upload/ and upload a JPG.
