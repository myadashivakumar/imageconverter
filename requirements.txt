fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
img2pdf==0.6.3
pikepdf==9.3.0
jinja2==3.1.4
python-docx==1.2.0
reportlab==5.0.0
pdf2docx==0.5.13
# Pinned: the latest opencv-python-headless (5.x) ships no macOS wheel yet and
# falls back to a slow from-source build. 4.10.0.84 has prebuilt wheels.
opencv-python-headless==4.10.0.84
